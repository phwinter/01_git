 
# i: name bearbeiten; esc um raus zu kommen 
#w: speichern
#q: beenden


#git branch, git merge, git status
# git add "Rfile" , git commit (statt commit und speichern drücken)
#Rückgängig machen: git revert "SHA vom ausgewählten Punkt im branch" eher nicht verwenden
# git checkout